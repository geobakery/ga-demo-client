VITE_API_URL=your_api_url # Base URL incl. version, e.g. http://localhost/ga/v2
VITE_DATA_EXTENT=saxony # "saxony" (default) shows the state border, "demo" the demo-data bounding box. The map starts zoomed to the chosen extent.
